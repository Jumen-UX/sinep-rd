name: Temporary source snapshot

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  snapshot:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Create snapshot
        run: |
          zip -qr repo-source-snapshot.zip . \
            -x '.git/*' 'node_modules/*' '.next/*' 'repo-source-snapshot.zip'

      - name: Commit snapshot
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add repo-source-snapshot.zip
          git commit -m 'chore: temporary source snapshot [skip ci]'
          git push
